# Generated by direnv_smart_setup.sh
# --- Auto-generated .envrc for venv ---
layout python "python3"
export VENV_NAME="venv-bau-ticket"

if [ -d ".venv/bin" ]; then
    source .venv/bin/activate
fi

if [ -f requirements.txt ] && { [ ! -f .requirements_installed ] || [ requirements.txt -nt .requirements_installed ]; }; then
    echo "📦 Installing requirements..."
    ./install_requirements.sh requirements.txt venv
else
    if [ ! -f .requirements_installed ]; then
        echo "✨ Installing default tools (black, flake8, isort)..."
        pip install black flake8 isort
        touch .requirements_installed
    fi
fi

export PYTHONPATH="$PWD:${PYTHONPATH:-}"
